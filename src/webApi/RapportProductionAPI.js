import config from '@/webapi/config.js';
import axios from 'axios';
import { promises } from 'fs';

const instance = axios.create({
    baseURL: 'http://localhost:53947/api/',
   });
   
 
  export default {
   
  
  }
